<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- التطبيق يعمل على الهاتف والتلفاز معاً من نفس الحزمة: يبقى Leanback اختيارياً (required=false)
         حتى لا يستبعد متجر Play التطبيق من أجهزة الهاتف العادية، ونفس الشيء بالنسبة لشاشة اللمس -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".FlixApplication"
        android:allowBackup="true"
        android:banner="@drawable/tv_banner"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- يفتح التطبيق مباشرة عند مسح رمز QR ربط سطح المكتب بكاميرا الهاتف العادية
                 (autoVerify يتطلب رفع assetlinks.json على النطاق ليعمل بدون قائمة اختيار تطبيق) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="app.flixplayer.pro"
                    android:pathPrefix="/pair.php" />
            </intent-filter>
        </activity>

        <!-- نقطة دخول مخصصة لأجهزة تلفزيون Android: نفس الحزمة والبيانات، بواجهة مصممة لمسافة
             المشاهدة (10-foot UI) والتنقل عبر جهاز التحكم (D-pad) بدل اللمس. أجهزة التلفاز تبحث
             تلقائياً عن فئة LEANBACK_LAUNCHER أولاً، وتتجاهل MainActivity الخاص بالهاتف -->
        <activity
            android:name=".tv.TvMainActivity"
            android:exported="true"
            android:banner="@drawable/tv_banner"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
